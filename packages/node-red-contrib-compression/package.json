{
  "name": "node-red-contrib-compression",
  "version": "0.1.1",
  "description": "Node-RED nodes for data compression, caching, and visualization",
  "keywords": [
    "node-red",
    "compression",
    "chart",
    "visualization",
    "cache"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "node-red": {
    "version": ">=2.0.0",
    "nodes": {
      "compression": "nodes/compression.js",
      "cache": "nodes/cache.js",
      "chart": "nodes/chart.js"
    }
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "prepublishOnly": "npm test --if-present",
    "publish:dry": "npm publish --dry-run",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "dependencies": {}
}
